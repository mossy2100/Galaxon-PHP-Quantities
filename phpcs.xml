<?xml version="1.0"?>
<ruleset name="Galaxon">
    <description>Coding standard for Galaxon libraries</description>

    <!-- Paths to check -->
    <file>src</file>
    <file>tests</file>

    <!-- Exclude generated data files -->
    <exclude-pattern>src/Currencies/data</exclude-pattern>

    <!-- Use Galaxon PHP Coding Standard (includes PSR12 + custom rules) -->
    <rule ref="Galaxon"/>

</ruleset>
